<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- 配置注解驱动 -->
    <mvc:annotation-driven/>
    <!-- 配置资源映射 -->
    <mvc:resources mapping="/static/**" location="/WEB-INF/static/"/>
    <!-- 配置包扫描器 -->
    <context:component-scan base-package="com.nachepin.mobile.controller"/>
    <!-- 配置拦截器 -->
    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/app/**" />
            <!--<mvc:exclude-mapping path="/swagger-ui.html" />-->
            <mvc:exclude-mapping path="/app/customer/v1/login" />
            <bean class="com.nachepin.mobile.interceptor.BeforeLoginInterceptor"></bean>
        </mvc:interceptor>
        <mvc:interceptor>
            <mvc:mapping path="/app/**" />
            <!--<mvc:exclude-mapping path="/swagger-ui.html" />-->
            <mvc:exclude-mapping path="/app/customer/v1/login" />
            <mvc:exclude-mapping path="/app/goods/**" />
            <bean class="com.nachepin.mobile.interceptor.BeforeLoginInterceptor"></bean>
        </mvc:interceptor>
    </mvc:interceptors>

    <bean class="springfox.documentation.swagger2.configuration.Swagger2DocumentationConfiguration" id="swagger2Config"/>
    <mvc:resources location="classpath:/META-INF/resources/" mapping="swagger-ui.html"/>
    <mvc:resources location="classpath:/META-INF/resources/webjars/" mapping="/webjars/**"/>

</beans>